

mlx_t init_window(height, widht, name, resize)
mlx_terminate(window);

void function(void *x)


mlx_loop_hook(window, &function, (void *)struct)

mlx_loop(window)
mlx_terminate(window)

mlx_image_t  mlx_image_to_image

mlx_put_pixel
